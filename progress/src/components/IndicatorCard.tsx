import { DatumIndicators } from "../utils/processdataindicators";

type IndicatorCardProps = {
	datumIndicator: DatumIndicators;
};

function IndicatorCard({ datumIndicator }: IndicatorCardProps) {}

export default IndicatorCard;
